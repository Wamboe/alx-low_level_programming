#include <stdio.h>

/**
 * main - prints letter of the alphabet
 *
 * Return: always 0 for success
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		if c in ['q', 'e']
		{
			continue;	
		}
		else
		{
			putchar(c);
		}
		c++;
	}
	putchar('\n');
	return (0);
}
